var names = ["Ronit", "Ayush", "Sijan", "Adhisthit", "Ashok", "Sharams", "Sriyam", "Peter", "Raaj", "Jim"];
for (var i = 0; i < names.length; i++) {
    byeSpeaker.speak(names[i]);
  } else {
    helloSpeaker.speak(names[i]);
  }
}

})();
